"""Phase 2 placeholder — Claude tool definitions for the agent loop.

Intentionally empty in Phase 1. Each tool will live in its own module
under this package (one file per tool, matching the structure used by
`app/services/`).
"""
