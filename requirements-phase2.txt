# --- NOT the production install file. ---
# Railway installs from `requirements.txt` (see Procfile / railway.json /
# .github/workflows/ci.yml). This file lists optional / future dependencies
# staged for Phase 2/3 work. Move a line up to `requirements.txt` only once
# the corresponding code has actually landed in `app/`.

-r requirements.txt

# --- Background jobs (Celery worker + Beat scheduler) ---
celery==5.4.0
redis==5.2.0

# --- External integrations ---
tavily-python==0.5.0

# --- Utilities ---
humanize==4.11.0
