# Optional / future dependencies. Add to requirements.txt (or include via -r)
# only once the corresponding code lands in app/.

-r requirements.txt

# --- Anthropic Claude (AI assistant / agent) ---
anthropic==0.39.0

# --- Background jobs (Celery worker + Beat scheduler) ---
celery==5.4.0
redis==5.2.0

# --- External integrations ---
tavily-python==0.5.0

# --- Utilities ---
humanize==4.11.0
