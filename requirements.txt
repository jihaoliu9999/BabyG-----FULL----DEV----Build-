# babyg runtime dependencies — only what app/ actually imports.
# Optional deps (celery, redis, tavily, humanize) live in
# requirements-phase2.txt and only ship when the corresponding code arrives.

# --- Web framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
gunicorn==23.0.0
python-multipart==0.0.12
jinja2==3.1.4
itsdangerous==2.2.0

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- HTTP & async ---
httpx==0.27.2

# --- Supabase ---
supabase==2.10.0

# --- Image processing (profile photos) ---
pillow==11.0.0

# --- AI assistant / agent ---
anthropic==0.39.0
