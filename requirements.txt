# babyg Phase 1 runtime dependencies — only what app/ actually imports.
# Phase 2 deps (anthropic, celery, redis, twilio, resend, posthog, sentry,
# structlog, humanize, tavily) live in requirements-phase2.txt and only
# ship when the corresponding code arrives.

# --- Web framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
gunicorn==23.0.0
python-multipart==0.0.12
jinja2==3.1.4
itsdangerous==2.2.0

# --- Config & validation ---
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- HTTP & async ---
httpx==0.27.2

# --- Supabase ---
supabase==2.10.0

# --- Phase 2 AI assistant ---
anthropic==0.39.0
